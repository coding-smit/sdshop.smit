services:
  - type: web
    name: django-hello-app
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn hello.wsgi:application"
